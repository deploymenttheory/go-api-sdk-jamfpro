package main

import (
	"fmt"
	"log"

	"github.com/deploymenttheory/go-api-sdk-jamfpro/sdk/jamfpro"
)

func main() {
	// Define the path to the JSON configuration file
	configFilePath := "/Users/dafyddwatkins/localtesting/jamfpro/clientconfig.json"

	// Initialize the Jamf Pro client with the HTTP client configuration
	client, err := jamfpro.BuildClientWithConfigFile(configFilePath)
	if err != nil {
		log.Fatalf("Failed to initialize Jamf Pro client: %v", err)
	}

	// Create a new jamfpro client instance
	client, err := jamfpro.BuildClient(config)
	if err != nil {
		log.Fatalf("Failed to create Jamf Pro client: %v", err)
	}

	// Define the name of the subscription to delete.
	nameToDelete := "Example Volume Purchasing Subscription"

	err = client.DeleteVolumePurchasingSubscriptionByName(nameToDelete)
	if err != nil {
		fmt.Printf("Error deleting volume purchasing subscription by name: %s\n", err)
		return
	}

	fmt.Println("Volume purchasing subscription deleted successfully.")
}
